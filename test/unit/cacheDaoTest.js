const should = require('should');

const cacheService = require('../../app/services/CacheService');

// A dummy test.
describe('Unit: CacheDao Testing', () => {
    describe('getByKeyOrInsert', () => {
        it('Not existing key return valid object', (done) => {
            done();
        });
    });
});